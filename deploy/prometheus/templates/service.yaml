apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: leo-telemetry
  labels:
    {{- include "prometheus.labels" . | nindent 4 }}
spec:
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: http
