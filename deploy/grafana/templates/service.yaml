apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: leo-telemetry
  labels:
    {{- include "grafana.labels" . | nindent 4 }}
spec:
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
