name: Build & Push Ingest Image

on:
  push:
    branches: [main]
    paths:
      - "leo_telemetry/**"
      - "Dockerfile"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/build-ingest.yml"

permissions:
  contents: write
  packages: write
  pull-requests: write

# Only the newest main build should pin a deployment image. Without this,
# overlapping runs create PRs that edit the same image.tag fields and the
# older PR can conflict after the newer one merges.
concurrency:
  group: build-ingest-main
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/leo-telemetry-ingest

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run tests
        run: uv run pytest

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Build and tag exactly the commit that triggered this run.
          ref: ${{ github.sha }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install yq
        run: |
          curl -sL "https://github.com/mikefarah/yq/releases/download/v4.44.3/yq_linux_amd64" -o yq
          chmod +x yq
          sudo mv yq /usr/local/bin/yq

      - name: Base the manifest update on the latest main
        run: |
          git fetch origin main
          git checkout --detach origin/main

      - name: Pin the deployed image to this commit
        run: |
          yq -i '.image.tag = "${{ github.sha }}"' deploy/ingest/values.yaml
          yq -i '.image.tag = "${{ github.sha }}"' deploy/decode/values.yaml
          yq -i '.image.tag = "${{ github.sha }}"' deploy/demux/values.yaml
          yq -i '.image.tag = "${{ github.sha }}"' deploy/observability/values.yaml

      - name: Open and merge the manifest-pin PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add deploy/ingest/values.yaml deploy/decode/values.yaml deploy/demux/values.yaml deploy/observability/values.yaml
          if git diff --cached --quiet; then
            echo "No manifest changes to commit"
            exit 0
          fi

          # main requires a PR (repo ruleset) -- direct pushes are rejected,
          # so open a short-lived branch/PR and merge it immediately instead
          # of trying to bypass the rule. required_approving_review_count is
          # 0, so this needs no human in the loop.
          branch="ci/pin-images-${{ github.sha }}"
          git checkout -b "$branch"
          git commit -m "deploy: pin service images to ${{ github.sha }}"
          git push origin "$branch"

          pr_url=$(gh pr create --base main --head "$branch" \
            --title "deploy: pin service images to ${{ github.sha }}" \
            --body "Automated tag pin from the image build.")
          gh pr merge "$pr_url" --merge --delete-branch
